name: Desplegar a GitHub Pages

on:
  push:
    branches: [main]
    paths:
      # El sitio no cambia porque cambio un scraper. Sin este filtro, cada
      # ajuste de Python dispararia un deploy inutil.
      - "web/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - id: pages
        uses: actions/configure-pages@v5

      - run: npm ci
        working-directory: web

      - name: Construir sitio
        working-directory: web
        env:
          # La key publishable es publica por diseno (viaja al navegador), pero
          # igual vive en Secrets para no tenerla escrita en el repo.
          PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
          PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}
          # Pages sirve un repo de proyecto bajo /nombre-del-repo/. Sin el base
          # correcto, todos los enlaces internos dan 404. `base_url` sale del
          # propio deploy, asi que no hay que hardcodear el usuario.
          PUBLIC_SITE_URL: ${{ steps.pages.outputs.origin }}
          PUBLIC_BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
